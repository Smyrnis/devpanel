use iced::widget::text_editor;

#[derive(Debug, Clone)]
pub enum VHostsMessage {
    Scan,
    ScanDone(Vec<crate::domain::vhosts::VHostEntry>),
    ShowAddForm,
    HideForm,
    FormServerNameChanged(String),
    FormDocRootChanged(String),
    FormPhpVersionChanged(Option<String>),
    FormHttpsChanged(bool),
    Create,
    CreateDone(bool, String),
    EditRequest(usize),
    SaveEdit,
    SaveEditDone(bool, String),
    OpenBrowser(String),
    OpenDocumentRoot(String),
    OpenDevpanelConf,
    ToggleExpanded(usize),
    DeleteRequest(usize),
    DeleteConfirm(usize),
    BulkDeleteConfirm,
    DeleteCancel,
    DeleteDone(bool, String),
    ToggleSelected(usize),
    SearchChanged(String),
    SelectAll,
    ClearSelection,
    BulkTagChanged(String),
    ApplyBulkTag,
    ToggleHttps(usize),
    DuplicateRequest(usize),
    OpenConfigEditor,
    CloseConfigEditor,
    ConfigLoaded(String),
    ConfigFileChanged,
    ConfigEditorAction(text_editor::Action),
    SaveConfigFile,
    SaveConfigDone(bool, String),
}

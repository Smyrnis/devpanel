name: CI

on:
  push:
    branches:
      - main
      - development
  pull_request:

jobs:
  checks:
    name: Rust and script checks
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            pkg-config \
            libfontconfig1-dev \
            libgl1-mesa-dev \
            libwayland-dev \
            libx11-dev \
            libxi-dev \
            libxkbcommon-dev \
            libxrandr-dev

      - name: Use stable Rust
        run: |
          rustup update stable
          rustup default stable

      - name: Check formatting
        run: cargo fmt --all -- --check

      - name: Check build
        run: cargo check

      - name: Run clippy
        run: cargo clippy --all-targets -- -D warnings

      - name: Run tests
        run: cargo test

      - name: Install cargo-deb
        run: cargo install cargo-deb --locked

      - name: Build Debian package
        run: cargo deb

      - name: Verify Debian package
        run: scripts/verify-deb.sh target/debian/devpanel_*.deb

      - name: Check installer shell syntax
        run: |
          bash -n scripts/postinst
          bash -n scripts/devpanel-setup.sh
          bash -n tests/shell/project_permissions_test.sh
          bash -n tests/shell/runtime_installers_test.sh
          bash -n installation/devpanel-setup.sh
          bash -n installation/devpanel-create-projects-dir.sh
          for file in installation/lib/*.sh installation/dependencies/*.sh; do
            bash -n "$file"
          done

      - name: Test runtime installer scripts
        run: |
          tests/shell/project_permissions_test.sh
          tests/shell/runtime_installers_test.sh
